/* tslint:disable */

const sqlite = require("sqlite3").verbose();

const actorDBList = [];

export const openDB = (s: string) => {
  
};

export const getDBData = (index: number, s: string) => {
  
};

export const runDBSql = (index: number , s: string) => {
  
};
